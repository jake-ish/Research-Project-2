# Raw third-party data — not redistributed by default (see data/DATA_SOURCES.md)
data/raw/*
!data/raw/.gitkeep
# ...except the reference CDS FASTAs, committed so the pipeline runs out of the box
# (SGD R64-2-1 and NCBI RefSeq NC_000913.2 are openly redistributable)
!data/raw/orf_coding_all_R64-2-1_20150113.fasta
!data/raw/NC_000913_2_cds_nucl.fasta

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.ipynb_checkpoints/

# PAML working / scratch files
*.ctl
2ML.*
2NG.*
rst
rst1
rub
yn00.out
codeml.out
lnf
*.paml.tmp

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Large intermediates (uncomment if needed)
# *.fasta
# *.aln

# Secrets / local config — never commit these
.env
*.key
ncbi_api_key*
